# Capture — a preference, not a fact

Noticed this week: when Claude gives me three options with tradeoffs, I
almost always just want its recommendation. Want: "recommend one, note
the strongest alternative in a sentence" as the default, options-menus
only when I explicitly ask.

(Triage hint: this is the classifier's always-load case — it should
shape every session, so it belongs in
`02-context/communication-style.md`, not in a note that only loads
on-demand. Good live demo of the classifier.)
